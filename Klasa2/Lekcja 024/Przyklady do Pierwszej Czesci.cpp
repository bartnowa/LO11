#include <iostream>

using namespace std;

int main()
{
    /*
    //Przyklad 7
    int k, s=0, d=35;
    while (d>0)
    {
        k = d % 2;
        s = s + k;
        d = d / 2;
    }
    cout << s;

    /*
    //Przyklad 6
    int z = 60;
    for (int i=0 ; i<15 ; i++)
    {
        if (i%3==0) z = z / 2;
    }
    cout << z;

    /*
    //Przyklad 5
    int w=1, p=5;
    for (int i=0 ; i<p ; i++)
    {
        w = w * 2;
    }
    cout << w;

    /*
    // Przyklad 4
    int k = 5;
    int n = 4;
    int m = 2*k + n;
    if (m%k>3) n = n*n;
        else n = n*n*n;
    for (int i=1 ; i<k ; i++)
    {
        m++;
    }
    cout << k << " " << n << " " << m;

    /*
    // Przyklad 3
    int c = 10 % 18;
    for (int i=0 ; i<5 ; i++)
    {
        if (c>=10) c = c * 2;
    }
    cout << c;

    /*
    // Przyklad 2
    int w = 12;
    for (int j=10 ; j>=5 ; j--)
    {
        if (j%2==0) w = w + 2;
            else w = w / 2;
    }
    cout << w;

    /*
    // Przyklad 1
    int k = 4;
    for (int i=3 ; i<6 ; i++)
        k = k + 2*i;
    cout << k;
    */
}
